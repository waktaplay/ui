import Normal from "./sub-components/Normal"
import Medium from "./sub-components/Medium"
import Small from "./sub-components/Small"

const Chip = Object.assign(Normal, { Normal, Medium, Small })

export default Chip
